[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "cd packages/aba-payway-sdk-unofficial && npm run build",
  "cd apps/api && npm run build"
]

[start]
cmd = "cd apps/api && npm start"
